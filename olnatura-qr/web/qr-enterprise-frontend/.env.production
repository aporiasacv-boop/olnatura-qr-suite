# Producción: SPA embebida en el mismo origen que la API (JAR / App Service Azure).
# Vacío = rutas relativas (recomendado). El host Azure es el mismo que sirve el JAR:
#   https://olnaturaqr-dchrfye3fgfzcjbn.mexicocentral-01.azurewebsites.net
VITE_API_BASE_URL=
