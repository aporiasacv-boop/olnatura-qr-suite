# Copia este archivo a .env.local para pruebas en LAN.
# .env.local está en .gitignore y no se sube al repo.
#
# cp .env.local.example .env.local
# (o en PowerShell: Copy-Item .env.local.example .env.local)

VITE_API_BASE_URL=http://192.168.41.230:3001
